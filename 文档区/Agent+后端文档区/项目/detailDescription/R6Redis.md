# Redis

Agent中的Redis使用往往是 保存Cache的时候, 

## 常见的问题:

### 1.缓存击穿

> 一个访问量很高的缓存 Key 突然失效，大量并发请求同时发现缓存未命中，于是一起访问数据库或执行昂贵计算。

可能出现的原因是:  TTL 到期 / Redis 内存不足被淘汰 / 主动删除 / Redis 重启或故障 / Cache Key 版本发生变化

#### 1.1 


