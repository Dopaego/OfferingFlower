# 浏览器原理

## 浏览器为何采用多进程架构？单进程浏览器有什么缺陷？

- 单进程浏览器缺陷：
  - 不稳定： 插件崩溃、渲染线程崩溃会导致整个浏览器崩溃。
  - 不流畅： 页面上某个脚本死循环（如 JS 死循环）或插件卡死会阻塞整个浏览器，导致所有页面无响应。
  - 不安全： 插件或页面脚本可以轻易获取系统任意资源，易被利用进行恶意操作。
- 多进程架构解决方案：
  - 解决不稳定： 每个标签页（渲染进程）、每个插件都有独立的进程。一个标签页或插件崩溃，只会影响自身进程，不会影响浏览器主进程和其他页面/插件进程。
  - 解决不流畅： 脚本死循环只会阻塞它所在的渲染进程（标签页），其他页面标签运行不受影响。
  - 解决不安全： 使用沙箱 (Sandbox) 技术。沙箱可以看作是操作系统给进程加的限制。运行在沙箱中的程序（如渲染进程）被严格限制：无法读写硬盘数据、无法访问敏感的系统资源。当渲染进程需要访问网络或文件系统时，必须通过浏览器主进程进行代理访问，结果再通过 IPC 返回给渲染进程。

## 浏览器间进程是如何通信的？

浏览器采用多进程架构，进程之间不能互相访问内存，而是通过IPC进程间通信来协作传递数据和信息的：
比如渲染进程运行在沙箱里，没有权限直接访问网络和系统资源，需要通过 IPC 向浏览器主进程发起请求，主进程做安全校验并代理执行，再把结果通过 IPC 返回。
像网络进程下载资源后传给渲染进程、提交文档、合成图层等关键流程，都是靠 IPC 完成的，它既保证了进程隔离的安全稳定，又实现了进程间的协同工作。

### IPC的实现方式

- 管道： 单向通信，数据从一端写入另一段读出，
  - 匿名管道：适用于有亲缘关系的进程，比如父子进程
  - 命名管道：在文件系统中有名字，不相关的进程也可以使用。
- 消息队列： 允许进程发送和接收带类型的消息，消息在队列里排队，接收方可以按类型选择性地读取。比管道灵活，但每条消息大小有限制，Linux默认是8KB
- 共享内存：效率最高的IPC方式，多个进程直接读写同一块物理内存，不需要数据拷贝，但没有内置同步机制，必须配合信号量和互斥锁使用，否则会出现竞态条件。高性能缓存系统 经常用共享内存
- 信号量：本身不传递数据，用来同步，可以控制多个进程对共享资源的访问顺序，实现互斥或者限流
- 信号：异步通知机制，用来告诉进程发生了某个事情，只能传递一个信号编号，不能带复杂数据
- 套接字：最灵活，既能在本机进程间通信，也能跨网络通信，Unix域套接字用于本机，效率比TCP高。网络套接字用TCP/UDP协议，是分布式系统通信的基础。

### Electron原生的进程通信

<details class="lake-collapse"><summary id="u24a8f612"><span class="ne-text">豆包整理</span></summary><h3 id="xkMzR"><span class="ne-text">Electron 进程通信：核心机制与实现方式</span></h3><p id="u1bb9bfe1" class="ne-p"><span class="ne-text">Electron 基于 Chromium 多进程架构，核心分为</span><strong><span class="ne-text">主进程（Main Process）</span></strong><strong><span class="ne-text">和</span></strong><strong><span class="ne-text">渲染进程（Renderer Process）</span></strong><span class="ne-text">，两者之间的通信本质上是</span><strong><span class="ne-text">基于 Chromium 的 IPC（Inter-Process Communication）</span></strong><span class="ne-text">，Electron 对其做了上层封装，提供了更易用的 API。</span></p><hr id="HL54R" class="ne-hr"><h2 id="A6aTv"><span class="ne-text">一、Electron 进程通信的底层基础</span></h2><ul class="ne-ul"><li id="u7bfd8484" data-lake-index-type="0"><strong><span class="ne-text">底层依赖</span></strong><span class="ne-text">：Electron 复用了 Chromium 的 </span><strong><span class="ne-text">Mojo IPC</span></strong><span class="ne-text"> 或传统的 </span><code class="ne-code"><span class="ne-text">Named Pipe</span></code><span class="ne-text">/</span><code class="ne-code"><span class="ne-text">Socket</span></code><span class="ne-text">（不同平台实现不同），这是一种高效、可靠的进程间消息传递机制。</span></li><li id="ucf3a3f0f" data-lake-index-type="0"><strong><span class="ne-text">安全隔离</span></strong><span class="ne-text">：渲染进程运行在沙箱中，无法直接访问 Node.js API 和系统资源，必须通过 IPC 向主进程发起请求，主进程作为“代理”执行操作后再返回结果，这和浏览器的安全模型一致。</span></li></ul><hr id="azW3O" class="ne-hr"><h2 id="hRokW"><span class="ne-text">二、Electron 提供的核心 IPC API（面试必背）</span></h2><p id="u473a8efd" class="ne-p"><span class="ne-text">Electron 封装了两套核心 API，分别用于</span><strong><span class="ne-text">主 </span></strong><strong><span class="ne-text">↔</span></strong><strong><span class="ne-text"> 渲染</span></strong><span class="ne-text">和</span><strong><span class="ne-text">渲染 </span></strong><strong><span class="ne-text">↔</span></strong><strong><span class="ne-text"> 渲染</span></strong><span class="ne-text">通信：</span></p><h3 id="RCfeh"><span class="ne-text">1. 主进程 </span><span class="ne-text">↔</span><span class="ne-text"> 渲染进程通信（最常用）</span></h3><h4 id="D1T5v"><span class="ne-text">（1）单向通知：</span><code class="ne-code"><span class="ne-text">ipcMain</span></code><span class="ne-text"> / </span><code class="ne-code"><span class="ne-text">ipcRenderer.send</span></code></h4><ul class="ne-ul"><li id="ud8bd6947" data-lake-index-type="0"><span class="ne-text">渲染进程 → 主进程：</span></li></ul><pre data-language="javascript" id="SIGeg" class="ne-codeblock language-javascript"><code>// 渲染进程
ipcRenderer.send('channel-name', arg1, arg2)</code></pre><pre data-language="javascript" id="EDnmZ" class="ne-codeblock language-javascript"><code>// 主进程
ipcMain.on('channel-name', (event, arg1, arg2) =&gt; {
  console.log('收到渲染进程消息:', arg1, arg2)
})</code></pre><ul class="ne-ul"><li id="uefe46921" data-lake-index-type="0"><span class="ne-text">主进程 → 渲染进程：</span></li></ul><pre data-language="javascript" id="GDj37" class="ne-codeblock language-javascript"><code>// 主进程（需要拿到对应窗口的 webContents）
win.webContents.send('channel-name', data)</code></pre><pre data-language="javascript" id="f5GmI" class="ne-codeblock language-javascript"><code>// 渲染进程
ipcRenderer.on('channel-name', (event, data) =&gt; {
  console.log('收到主进程消息:', data)
})</code></pre><h4 id="cBNra"><span class="ne-text">（2）双向请求-响应：</span><code class="ne-code"><span class="ne-text">ipcRenderer.invoke</span></code><span class="ne-text"> / </span><code class="ne-code"><span class="ne-text">ipcMain.handle</span></code><span class="ne-text">（推荐）</span></h4><p id="ud13d1ee5" class="ne-p"><span class="ne-text">这是更现代的方式，基于 Promise，适合“请求-获取结果”的场景：</span></p><pre data-language="javascript" id="Dj7LL" class="ne-codeblock language-javascript"><code>// 渲染进程（发起请求，等待结果）
const result = await ipcRenderer.invoke('get-user-data', userId)</code></pre><pre data-language="javascript" id="kmVwX" class="ne-codeblock language-javascript"><code>// 主进程（处理请求，返回结果）
ipcMain.handle('get-user-data', async (event, userId) =&gt; {
  const user = await db.findUser(userId)
  return user // 自动包装为 Promise 返回给渲染进程
})</code></pre><div class="ne-quote"><p id="u51c50154" class="ne-p"><span class="ne-text">对比：</span><code class="ne-code"><span class="ne-text">send</span></code><span class="ne-text"> 是单向“发了就忘”，</span><code class="ne-code"><span class="ne-text">invoke</span></code><span class="ne-text"> 是双向“请求-响应”，更适合业务逻辑。</span></p></div><hr id="BcnCX" class="ne-hr"><h3 id="byTlt"><span class="ne-text">2. 渲染进程 </span><span class="ne-text">↔</span><span class="ne-text"> 渲染进程通信</span></h3><p id="u4fdd9e79" class="ne-p"><span class="ne-text">有两种方式：</span></p><ol class="ne-ol"><li id="ue7c3005c" data-lake-index-type="0"><strong><span class="ne-text">通过主进程中转</span></strong><span class="ne-text">（最安全、最常用）：</span></li></ol><ul class="ne-list-wrap"><ul ne-level="1" class="ne-ul"><li id="ue58411ca" data-lake-index-type="0"><span class="ne-text">渲染进程 A → 主进程 → 渲染进程 B</span></li><li id="ub3eb4220" data-lake-index-type="0"><span class="ne-text">主进程作为“消息路由器”，避免渲染进程直接通信，保持隔离性</span></li></ul></ul><ol start="2" class="ne-ol"><li id="u7a74396a" data-lake-index-type="0"><code class="ne-code"><span class="ne-text">MessagePort</span></code><strong><span class="ne-text">（直接通信）</span></strong><span class="ne-text">：</span></li></ol><pre data-language="javascript" id="svQEH" class="ne-codeblock language-javascript"><code>// 主进程
const { port1, port2 } = new MessageChannel()
win1.webContents.postMessage('port', null, [port1])
win2.webContents.postMessage('port', null, [port2])</code></pre><pre data-language="javascript" id="Ypisd" class="ne-codeblock language-javascript"><code>// 渲染进程
ipcRenderer.on('port', (event) =&gt; {
  const port = event.ports[0]
  port.onmessage = (e) =&gt; console.log('收到另一个渲染进程消息:', e.data)
  port.postMessage('Hello from renderer A')
})</code></pre><ul class="ne-list-wrap"><ul ne-level="1" class="ne-ul"><li id="uc8a8e31b" data-lake-index-type="0"><span class="ne-text">主进程创建 </span><code class="ne-code"><span class="ne-text">MessageChannel</span></code><span class="ne-text">，将端口分别传递给两个渲染进程</span></li><li id="ubc28ce9f" data-lake-index-type="0"><span class="ne-text">渲染进程之间可以直接通信，绕过主进程，性能更高</span></li></ul></ul><hr id="xVkQD" class="ne-hr"><h2 id="rULqV"><span class="ne-text">三、安全实践（面试加分项）</span></h2><p id="uff0d1a7c" class="ne-p"><span class="ne-text">Electron 进程通信如果处理不当，会引入安全风险，核心注意点：</span></p><ol class="ne-ol"><li id="ua7451716" data-lake-index-type="0"><strong><span class="ne-text">开启上下文隔离（contextIsolation: true）</span></strong><span class="ne-text">：</span></li></ol><ul class="ne-list-wrap"><ul ne-level="1" class="ne-ul"><li id="u1147feb9" data-lake-index-type="0"><span class="ne-text">渲染进程不能直接访问 Node.js，必须通过 </span><strong><span class="ne-text">preload 脚本</span></strong><span class="ne-text">暴露安全的 IPC 接口</span></li><li id="ubb355260" data-lake-index-type="0"><span class="ne-text">示例：</span></li></ul></ul><pre data-language="javascript" id="U2XwW" class="ne-codeblock language-javascript"><code>// preload.js
const { contextBridge, ipcRenderer } = require('electron')
contextBridge.exposeInMainWorld('myAPI', {
  getUserData: (userId) =&gt; ipcRenderer.invoke('get-user-data', userId)
})</code></pre><p id="u2e569ba3" class="ne-p"><span class="ne-text">渲染进程通过 </span><code class="ne-code"><span class="ne-text">window.myAPI.getUserData()</span></code><span class="ne-text"> 调用，避免直接暴露 </span><code class="ne-code"><span class="ne-text">ipcRenderer</span></code><span class="ne-text">。</span></p><ol start="2" class="ne-ol"><li id="ubbcfb633" data-lake-index-type="0"><strong><span class="ne-text">验证所有输入</span></strong><span class="ne-text">：</span></li></ol><ul class="ne-list-wrap"><ul ne-level="1" class="ne-ul"><li id="u7b3999fb" data-lake-index-type="0"><span class="ne-text">主进程必须校验渲染进程传来的参数，防止恶意代码注入</span></li><li id="uf9b5de4d" data-lake-index-type="0"><span class="ne-text">比如：</span><code class="ne-code"><span class="ne-text">userId</span></code><span class="ne-text"> 必须是数字，不能是 SQL 语句等</span></li></ul></ul><ol start="3" class="ne-ol"><li id="u1fb83be7" data-lake-index-type="0"><strong><span class="ne-text">最小权限原则</span></strong><span class="ne-text">：</span></li></ol><ul class="ne-list-wrap"><ul ne-level="1" class="ne-ul"><li id="u734a55c4" data-lake-index-type="0"><span class="ne-text">只暴露必要的 IPC 通道，不要让渲染进程能调用所有主进程能力</span></li></ul></ul><hr id="zlGUB" class="ne-hr"><h2 id="ySgvE"><span class="ne-text">四、面试话术总结</span></h2><p id="uf67c7fce" class="ne-p"><span class="ne-text">你可以这样回答面试官：</span></p><div class="ne-quote"><p id="ub300a2db" class="ne-p"><span class="ne-text">Electron 基于 Chromium 的多进程架构，主进程和渲染进程之间通过 </span><strong><span class="ne-text">IPC（进程间通信）</span></strong><span class="ne-text"> 协作，底层依赖 Chromium 的高效 IPC 机制（如 Mojo），上层封装为易用的 API。</span></p><p id="u0a5cd57d" class="ne-p"><span class="ne-text">核心通信方式有：</span></p><ol class="ne-ol"><li id="u2b00ffce" data-lake-index-type="0"><strong><span class="ne-text">主 </span></strong><strong><span class="ne-text">↔</span></strong><strong><span class="ne-text"> 渲染</span></strong><span class="ne-text">：用 </span><code class="ne-code"><span class="ne-text">ipcMain</span></code><span class="ne-text">/</span><code class="ne-code"><span class="ne-text">ipcRenderer</span></code><span class="ne-text">，单向通知用 </span><code class="ne-code"><span class="ne-text">send</span></code><span class="ne-text">，双向请求用 </span><code class="ne-code"><span class="ne-text">invoke</span></code><span class="ne-text">/</span><code class="ne-code"><span class="ne-text">handle</span></code><span class="ne-text">；</span></li><li id="u0683b4a7" data-lake-index-type="0"><strong><span class="ne-text">渲染 </span></strong><strong><span class="ne-text">↔</span></strong><strong><span class="ne-text"> 渲染</span></strong><span class="ne-text">：通常通过主进程中转，也可以用 </span><code class="ne-code"><span class="ne-text">MessagePort</span></code><span class="ne-text"> 实现直接通信；</span></li><li id="u6f42eb5d" data-lake-index-type="0"><strong><span class="ne-text">安全上</span></strong><span class="ne-text">：必须开启上下文隔离，通过 preload 脚本暴露有限接口，同时校验所有输入，避免安全风险。</span></li></ol></div><hr id="liryP" class="ne-hr"><p id="ub07b9002" class="ne-p"><span class="ne-text">要不要我帮你写一个</span><strong><span class="ne-text">最小可运行的 Electron IPC 示例代码</span></strong><span class="ne-text">（包含主进程、渲染进程、preload 脚本），你可以直接跑起来加深理解？</span></p></details>

### 渲染进程（浏览器内核）包含哪些核心线程？它们各自负责什么？协作关系如何？

- GUI 渲染线程：
  - 负责解析 HTML、CSS，构建 DOM 树、CSSOM 树、Render 树 (渲染树)，进行布局和绘制（像素填充），最终渲染出用户可见的页面。
  - 它与 JS 引擎线程是互斥的。当 JS 引擎线程在执行 JS 脚本时，GUI 渲染线程会被挂起（被“冻结”），必须等到 JS 引擎空闲时才能执行。所以 JS 执行时间过长会阻塞页面渲染（导致卡顿），需要尽量控制 JS 体积和执行时间。
- JS 引擎线程 (如 V8)：
  - 负责解析和执行 JavaScript 代码。V8 引擎本身是在 JS 引擎线程上运行的。
  - 一个渲染进程通常只有一个 JS 引擎线程。JS 引擎线程与 GUI 渲染线程互斥。
- 定时器触发线程：
  - 负责管理 `setTimeout` 和 `setInterval`。JS 引擎是单线程的，如果定时器的计时在 JS 引擎线程上运行，阻塞性的任务会影响计时的准确性。因此需要单独的线程来计时和触发。
  - 定时器到达指定时间后，计时线程会通过事件触发线程将定时器的回调函数添加到任务队列中。
- 事件触发线程：
  - 负责管理任务队列（Task Queue）。当一个事件需要被处理时（如定时器触发、异步请求完成、用户点击、I/O 操作完成），这个线程会把事件对应的回调函数添加到任务队列的队尾。
  - 等待 JS 引擎空闲时（即执行栈为空），事件循环机制会将任务队列中排在最前面的任务取出执行。
- 异步 HTTP 请求线程 (如 XMLHttpRequest)：
  - 当发起网络请求（如 `XMLHttpRequest` 或 `Fetch`）时，浏览器会新开一个线程来处理这个请求。
  - 该线程负责检测请求的状态变化（如完成、失败）。当状态发生变化并且请求对象设置了回调函数（如 `onreadystatechange`）时，该线程会将这个回调函数作为一个事件通知事件触发线程，由它添加到任务队列中等待 JS 引擎执行。

###  什么是事件循环 (Event Loop)？宏任务 (macrotask) 和微任务 (microtask) 有什么区别？执行顺序是什么？
+ 事件循环 (Event Loop): 是浏览器或 Node.js 解决 JS 单线程运行时异步任务不阻塞主线程的机制。核心思想是：JS 引擎有一个主线程负责执行代码（形成执行栈）和一个Event Loop 线程负责与其它线程（I/O、计时器等）通信和管理事件队列。
+ 运行流程：
    - 执行一个宏任务： 从任务队列中取出最老的一个任务（通常是`<script>`标签本身的代码）开始执行（同步代码）。
    - 清空微任务队列： 该宏任务执行结束前或后（V8 引擎通常在该宏任务结束后立即清空微任务队列），JS 引擎会检查微任务队列（Microtask Queue）。如果微任务队列中有任务，JS 引擎会按添加顺序一次性全部执行完，直到微任务队列清空。
    - UI渲染（如需要）： 浏览器可能会根据策略进行 UI 渲染（将渲染结果提交到 GPU 绘制）。注意：渲染在微任务之后，下一个宏任务之前。
    - 进入下一轮循环： 开始执行下一个宏任务（从任务队列取下一个）。再次回到步骤 2（清空微任务队列）。如比无限循环。
+ 宏任务 (macrotask) vs 微任务 (microtask):
    - 来源：
        * 宏任务：主要由浏览器发起（脚本代码、UI 交互）。
        * 微任务：主要由 JavaScript 自身发起（Promise、MutationObserver）。
    - 执行时机：
        * 微任务：在本轮事件循环的宏任务执行完、UI 渲染前，一次性执行完所有微任务。
        * 宏任务：在下一轮事件循环开始时执行。
    - 常见类型：
        * 宏任务： `<script>`整体代码、`setTimeout`、`setInterval`、`setImmediate`(Node)、I/O (鼠标事件、键盘事件、文件读写)、UI Rendering (UI 渲染本身不是宏任务，但触发的回调如 `resize` 是)、`postMessage`。
        * 微任务： `Promise.then()`/`Promise.catch()`/`Promise.finally()` 的回调、`process.nextTick`(Node, 优先级最高)、`MutationObserver` 的回调（已废弃的 `Object.observe`）。
    - 核心差异： 一个事件循环周期中，当执行栈为空时，会先检查并清空所有微任务，然后才会取下一个宏任务执行（也可能先做 UI 渲染）。微任务具有“插队”的能力。

###  详细描述从在浏览器地址栏输入 URL 到页面最终渲染完成的整个过程（涵盖导航阶段和渲染阶段）。
- [ ] **1. 从输入 URL 到页面渲染 (Navigation Flow)**
    - [ ] **网络阶段**：DNS 解析 (递归/迭代) -> TCP 3次握手 -> HTTP 请求/响应。
    - [ ] **解析阶段**：HTML -> DOM Tree, CSS -> CSSOM Tree.
    - [ ] **渲染阶段 (Critical Rendering Path)**：
        - [ ] **Render Tree**：DOM + CSSOM (不可见节点如 `display: none` 不在树中)。
        - [ ] **Layout (回流)**：计算几何信息 (位置、大小)。
        - [ ] **Paint (重绘)**：填充像素 (颜色、阴影)。
        - [ ] **Composite (合成)**：多个图层 GPU 合成 (Transform/Opacity 独立图层)。

#### 🅰️ 导航阶段 (Network & Process)： 浏览器进程 网络进程相关
1. 用户输入 & URL 处理：
    1. 用户在地址栏输入内容（URL 或搜索关键字）。
    2. 如果是搜索内容： 浏览器使用默认搜索引擎合成新的带搜索关键字的 URL。
    3. 如果是 URL： 浏览器根据规则（如检查协议）将其组装成完整的 URL（可能补全 `http://` 或 `https://`）。

<details class="lake-collapse"><summary id="ufbf1ae91"><span class="ne-text">判断是否是URL</span></summary><p id="u6f38add6" class="ne-p"><span class="ne-text">判断输入内容是否符合URL格式规则来区分，比如说是否包含&quot;://&quot;，域名格式是否正确</span></p></details>
2. 查找缓存：
    1. 网络进程检查本地缓存（包括浏览器缓存、Service Worker 缓存等）。<font style="background-color:#FBDE28;">补充浏览器缓存和ServiceWorker缓存</font>

<details class="lake-collapse"><summary id="u752b7cb8"><span class="ne-text" style="background-color: #FBDE28">浏览器缓存和ServiceWorker缓存</span></summary><p id="u282e6b3c" class="ne-p"><img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1773070975721-15d9cdc4-29a0-43f8-9639-1baaeea770fb.png" width="667" title="" crop="0,0,1,1" id="ud32f876f" class="ne-image"></p></details>
    2. 检查资源是否新鲜（如通过 `Cache-Control`/`Expires` 检查强缓存，通过 `If-Modified-Since`/`If-None-Match` 检查协商缓存）。【先检查强缓存，再检查协商缓存】。
    3. 若缓存有效，直接使用缓存资源，跳转至准备渲染进程。否则继续。
3. DNS 域名解析：
    1. 网络进程解析 URL 中的域名对应的 IP 地址【目标服务器的IP地址】。
    2. 查询顺序：浏览器缓存 → 操作系统缓存/ hosts 文件 → 本地 DNS 服务器 → 根 DNS 服务器/顶级域 DNS 服务器/权威 DNS 服务器（递归/迭代查询）。

<img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1772959728832-4f84b763-5522-4f49-83da-11d72e11c83b.png" width="699.0908939385221" title="" crop="0,0,1,1" id="u3fe7a257" class="ne-image">

4. 获取MAC地址：  
  当浏览器得到 IP 地址后，数据传输还需要知道⽬的主机 MAC 地址，  
  因为应⽤层下发数据给传输层，TCP 协议会指定源端⼝号和⽬的端⼝号  
  然后下发给⽹络层。⽹络层会将本机地址作为源地址，获取的 IP 地址作为⽬的地址。  
  然后将下发给数据链路层，数据链路层的发送需要加⼊通信双⽅的 MAC 地址，本机的 MAC 地址作为源 MAC 地址，⽬的 MAC 地址需要分情况处理。  
  **通过将IP地址与本机的⼦⽹掩码相与，可以判断是否与请求主机在同⼀个⼦⽹⾥，**  
  如果在同⼀个⼦⽹⾥，可以使⽤ ARP 协议获取到⽬的主机的 MAC 地址，  
  如果不在⼀个⼦⽹⾥，那么请求应该转发给⽹关，由它代为转发，  
  此时同样可以通过 ARP 协议来获取⽹关的 MAC 地址，此时⽬的主机的 MAC 地址应该为⽹关的地址。
5. 等待 TCP 连接 & 三次握手：
    1. 浏览器同一域名下最多允许建立约 6 - 8 个 TCP 连接。如果当前连接数已满，请求需排队等待。
    2. 进行 TCP 三次握手建立可靠连接（避免资源浪费）。
6. 发送 HTTP(S) 请求：
    1. 建立连接后，浏览器构建 HTTP 请求报文（请求行：方法+URL+HTTP版本、请求头、请求体），通过该连接发送给服务器。
    2. HTTPS 连接会先进行 TLS/SSL 握手协商加密参数。
    3. 连接构建好了，浏览器把用户的请求组成http报文（应用层数据）
    4. 协议栈封装

<details class="lake-collapse"><summary id="u421b9a9a"><span class="ne-text">协议栈封装的具体过程</span></summary><p id="u636d3890" class="ne-p"><img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1772958184594-1e17d1b5-94e9-424f-8d1f-6702ad0bb3dc.png" width="684.5454397083317" title="" crop="0,0,1,1" id="uc3bcc333" class="ne-image"></p></details>
然后网络上开始传输当前的请求

<details class="lake-collapse"><summary id="ubfe9f7fb"><span class="ne-text">传输过程</span></summary><p id="ud0af94a5" class="ne-p"><img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1772958317560-6e4e47cc-a154-4ad6-b92b-ed21a3612a43.png" width="684.5454397083317" title="" crop="0,0,1,1" id="u6c0ee2ac" class="ne-image"></p><p id="ud1409fdb" class="ne-p"><span class="ne-text" style="background-color: #FBDE28">路由器对数据包进行分片</span></p></details>
7. 服务器处理请求 & 返回响应：
    1. 服务器接收请求并处理。

<details class="lake-collapse"><summary id="udd257139"><span class="ne-text">服务端解析包的过程</span></summary><p id="u6d8d9c15" class="ne-p"><img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1772958373050-8d9b6484-36d4-4d4d-846f-350843901bca.png" width="670.9090763675283" title="" crop="0,0,1,1" id="ue6392a1b" class="ne-image"></p></details>
    2. 构建 HTTP 响应报文（状态行：状态码+状态描述、响应头、响应体）。
8. 浏览器接收响应 & 处理：
    1. 网络进程接收响应数据。
    2. 检查状态码：
        * `301`/`302`：重定向。从响应头 `Location` 中获取新地址，跳转到第一步（处理新 URL）。<font style="background-color:#FBDE28;">请求体是否保存</font>

<img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1773071036006-38ecc0d4-45d7-4416-830a-13cb60d18d4e.png" width="530.5" title="" crop="0,0,1,1" id="uc908f7b1" class="ne-image">

        * `200 OK`：检查响应头 `Content-Type`。
            + 若是 `text/html`：准备渲染 HTML。
            + 若是 `application/octet-stream`：触发下载流程。
    3. 如果请求资源类型是 HTML，准备进入渲染流程。
9. 准备渲染进程：
    1. 浏览器进程检查新页面的协议和根域名。
    2. 如果与某个已打开的页面相同，则复用该页面的渲染进程。否则，创建一个新的渲染进程。
10. 提交文档 (Commit Document)：
    1. 浏览器进程通过 IPC 向渲染进程发送“提交文档”的消息。
    2. 渲染进程接收到消息后与网络进程建立数据传输连接（管道）。
    3. 渲染进程边接收网络进程传输的 HTML 数据，边开始解析。// 流式渲染，让用户更快看到页面内容
    4. 当渲染进程确认所有基础资源（HTML）已经接收到（不一定完全下载完毕），会通过 IPC 向浏览器进程发送“确认提交”的消息。
    5. 浏览器进程收到“确认提交”消息后，更新浏览器界面状态：
        * 更新地址栏 URL
        * 更新安全状态（如 HTTPS 锁）
        * 前进后退历史状态更新
        * Web 页面内容开始渲染（此时页面可能还是空白 <font style="background-color:#FBDE28;">页面先白屏再显示内容的原因</font>）。

<img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1773071088726-02805ed2-16eb-4d95-bcbe-0560fed285ab.png" width="711.5" title="" crop="0,0,1,1" id="aTzCk" class="ne-image">

#### 🅱️ 渲染阶段 (Rendering)：发生在提交文档后的渲染进程中
1. 构建 DOM 树： HTML 解析器将 HTML 字节流转换为 DOM 树 (Document Object Model)。
2. 构建 CSSOM 树： CSS 解析器处理 CSS 资源（外部/内联）构建 CSSOM 树 (CSS Object Model)。
    1. CSS 会阻塞渲染： 浏览器会阻塞页面的首次渲染直到 CSSOM 构建完成（建议将 CSS 放在 `<head>` 中）。
3. 执行 JavaScript:
    1. 在构建 DOM 树过程中，遇到 `<script>` 标签（没有 `async`/`defer` 属性）时，会暂停 DOM 树的构建。

<details class="lake-collapse"><summary id="ua2b4a2ab"><span class="ne-text">async和defer的区别</span></summary><p id="u34c09460" class="ne-p"><span class="ne-text">async是下载后立即执行，适用于独立的、不依赖其他脚本或者DOM结构的脚本</span></p><p id="uf8fb617a" class="ne-p"><span class="ne-text">defer是HTML解析完成后才执行，多个defer会按照在文档中出现的顺序执行，适用于依赖DOM或者需要按照顺序执行的脚本</span></p></details>
    2. JS 引擎线程会下载（如果外部 JS）并执行该脚本（期间 GUI 渲染线程被挂起）。
    3. JS 可能修改已解析的 DOM 或 CSSOM，因此需要阻塞。
    4. JS 会阻塞解析： 建议将非关键 JS 放在底部或用 `async`/`defer` 属性、将<script> 放在body最底部
4. 构建 Render 树 (渲染树 / Layout 树)： 结合 DOM 树 和 CSSOM 树，剔除不可见元素（如 `display: none`, `<head>`），生成描述页面最终可见内容和样式的 Render 树。visibility：hidden的元素也会被包含在渲染树里，它仍占据布局空间内容。
5. 布局 (Layout / Reflow)： 计算 Render 树中每个节点的精确位置（坐标）和尺寸 (宽、高)。计算相对坐标和绝对坐标。这个过程也称为“回流”。布局是复杂且昂贵的操作。
6. 分层 (Layer Creation)： 浏览器根据某些特定规则（如 3D 变换、`position: fixed`、`<video>`、`<canvas>`、`will-change`）将页面划分为多个图层 (Layer)，以便优化性能。当某个图层需要重新渲染后，只需要渲染当前图层，而不是整个页面，减少了重绘的范围。
7. 绘制 (Paint / Rasterization)： 为每个图层生成一系列的绘制指令列表（记录栅格化的操作），这阶段将 Render 树转换为屏幕上的像素，也称为“光栅化”。该阶段主要进行像素填充（颜色、阴影、渐变、图片绘制等）。<font style="background-color:#FBDE28;">现代浏览器提高渲染效率的操作</font>

<img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1773071161213-e497a1d0-af3b-4abb-84b1-dba2456bc8db.png" width="571.5" title="" crop="0,0,1,1" id="ueaa0fb81" class="ne-image">

8. 合成与显示 (Composite & Display)：
    1. 合成： 合成线程将不同图层分割成图块 (tiles)，并将这些图块提交给栅格化线程池。
    2. 栅格化： 栅格化线程在 GPU 进程中将图块转换为位图数据并存储在 GPU 内存中。速度非常快（优先栅格化视口附近内容）。
    3. 绘制四角描述表： 合成线程收集栅格化完成的图块信息（称为绘制四角描述表 / Draw Quads），描述它们应在屏幕的哪个位置绘制。
    4. 最终显示： 合成线程通过 IPC 将这些信息提交给浏览器主进程。浏览器主进程再将其传递给GPU 进程。GPU 进程最终将这些位图信息合成最终的完整图像，并通过显卡硬件显示在屏幕上。

<font style="background-color:#FBDE28;">为什么推荐transform做动画而不是改变width、left这些属性</font>

<img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1773071191235-0b9308a8-e934-448d-9be7-8e8f59a1de75.png" width="799" title="" crop="0,0,1,1" id="uef56124e" class="ne-image">

9. 最终： 四次挥手断开 TCP 连接（如果 HTTP 头没有指定保持连接 `Connection: keep-alive`）。

### JavaScript 作为单线程语言，如何实现异步操作？核心原理是什么？
+ 核心问题： JS 单线程意味着在同一时间只能执行一个任务。如果所有操作（尤其是耗时的如网络请求、定时器）都同步执行，会严重阻塞页面渲染和用户交互（卡死）。
+ 解决方案：浏览器提供的多线程环境与协作机制（事件循环）
    - 非阻塞异步 API： 浏览器提供了异步 API（如 `setTimeout`, `XMLHttpRequest`, `fetch`, DOM 事件监听等）。当 JS 引擎调用这些 API 时，调用后立即返回（避免阻塞 JS 线程继续执行后续同步代码）。
    - 多线程辅助： 浏览器内核为这些异步操作创建对应的辅助线程来完成任务：
        1. 浏览器中多个辅助线程：GUI渲染线程、定时器线程、事件触发线程、异步HTTP请求线程、渲染合成线程、GPU线程等。
        2. 例如：`setTimeout(callback, 1000)` 调用后，由定时器线程计时 1 秒。
        3. 例如：`xhr.send()` 调用后，由异步 HTTP 请求线程处理网络请求。
    - 事件队列（任务队列）： 辅助线程完成任务（如计时结束、网络请求返回、用户点击）后，会创建一个事件（如 `timeout`, `readystatechange`, `click`），并将这个事件关联的回调函数通知给事件触发线程，由它添加到对应的任务队列（宏任务队列）中排队。
    - 事件循环（Event Loop）： JS 引擎（主线程）不断循环检查两个地方：
        1. 执行栈（Call Stack）: 当前是否有同步任务在执行？
        2. 任务队列： 是否有待执行的异步任务回调？
    - 当前执行栈完全空时，事件循环线程会从任务队列中取出最先进入队列的宏任务（一个）放入执行栈中执行。
    - JavaScript 执行该回调函数。
        * 如果在该宏任务的执行过程中产生了新的宏任务（如 `setTimeout`）或微任务（如 `Promise.then`），它们会被添加到相应的队列中。
        * 当前宏任务执行结束前/后，JS 引擎会清空整个微任务队列（按顺序执行所有微任务）。
        * （浏览器可能在此之后进行渲染）。
        * 然后进入下一轮循环。如此反复形成 “事件循环”。
+ 本质： JS 的单线程（执行栈）负责执行实际的 JS 逻辑，浏览器的多线程负责处理异步操作并将结果（回调函数）通过事件循环机制，在 JS 主线程空闲时安排执行栈执行。



### 沙箱 (Sandbox) 是如何保障渲染进程安全的？
+ 沙箱目的： 在渲染进程内部创建一个高度隔离的执行环境，限制渲染进程的能力，防止渲染页面（可能来自不可信来源）的代码对用户操作系统造成安全威胁（如读取文件、安装木马）。
+ 实现机制：
    - 权限剥离与限制： 沙箱将渲染进程的核心权限极大限制。默认情况下：
        1. 无权直接访问本地文件系统： 不能随意读写用户硬盘。
        2. 无权直接访问敏感系统资源： 不能访问注册表、其他进程内存等。
        3. 无权执行任意系统调用/命令。
    - IPC 通信代理：
        1. 当渲染进程（页面的 JS）确实需要执行受限制的操作时（例如发送网络请求、保存文件到用户选择的路径、访问 GPU 进行 3D 加速），它不能直接操作。
        2. 渲染进程必须通过 IPC (Inter-Process Communication) 向拥有更高权限的浏览器主进程发起请求。
        3. 浏览器主进程进行安全策略检查： 主进程接收请求后，会执行严格的安全策略检查（例如同源策略 CORS、文件选择器确认等），决定是否允许该操作。
        4. 操作代理与结果返回： 如果允许，浏览器主进程代替渲染进程执行该操作（如进行实际的网络请求或文件保存）。操作完成后，主进程将结果或操作是否成功的状态再次通过 IPC 发送回渲染进程。
    - 操作系统级隔离（部分实现）： 现代浏览器沙箱更进一步利用操作系统级别的安全特性（如 Windows 的 Job Objects、Linux 的 Namespaces/Seccomp-bpf、macOS 的 Seatbelt/sandbox-exec），在进程层面施加额外限制，使沙箱逃逸更加困难。
+ 总结： 沙箱通过权限限制、代理访问（IPC） 和操作检查，在渲染进程（及其内部的 JS 代码）与用户操作系统/敏感资源之间建立了一道坚固的隔离墙，只允许安全的、可控的操作通过代理通道执行。



### 前端安全问题
#### XSS跨站脚本攻击
<font style="color:rgb(37, 41, 51);">把恶意脚本注入到网页里，让浏览器把它当正常代码执行。攻击者可以偷 Cookie、劫持会话、钓鱼、甚至发起蠕虫攻击。</font>

<details class="lake-collapse"><summary id="ud529ad0f"><span class="ne-text" style="font-size: 16px">解决方案</span></summary><ol class="ne-ol"><li id="u45bfec28" data-lake-index-type="0"><span class="ne-text" style="color: rgb(37, 41, 51); font-size: 16px">不使用innerHTML 而是使用 textContent</span></li><li id="u348740fa" data-lake-index-type="0"><span class="ne-text" style="color: rgb(37, 41, 51); font-size: 16px">设置cookie时加上HttpOnly，js中拿不到cookie</span></li><li id="u333df0e4" data-lake-index-type="0"><span class="ne-text" style="color: rgb(37, 41, 51); font-size: 16px">Content-Security-Policy（CSP）： 通过HTTP响应头限制页面能加载哪些资源</span></li></ol><p id="u555f3dd1" class="ne-p"><img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1773647682961-8d962589-53fe-4187-95c8-38e308d52573.png" width="656.5" title="" crop="0,0,1,1" id="ud179cbdb" class="ne-image"></p></details>
#### CSRF跨站请求伪造
<font style="color:rgb(37, 41, 51);">用户登录了 A 网站，Cookie 里有会话信息。用户访问恶意网站 B，B 网站向 A 发请求，浏览器会自动带上 A 的 Cookie，A 网站以为是用户本人的操作。</font>

<details class="lake-collapse"><summary id="u485b6533"><span class="ne-text" style="font-size: 16px">解决方案</span></summary><ol class="ne-ol"><li id="u37bded73" data-lake-index-type="0"><span class="ne-text" style="color: rgb(37, 41, 51); font-size: 16px">Cookie 加上 </span><code class="ne-code"><span class="ne-text" style="color: rgb(255, 80, 44); background-color: rgb(255, 245, 245)">SameSite</span></code><span class="ne-text" style="color: rgb(37, 41, 51); font-size: 16px"> 属性，跨站请求就不会带 Cookie</span></li></ol><p id="u34fa5518" class="ne-p"><img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1773647789484-a0a82584-6600-42f8-b3eb-875741d43fee.png" width="664" title="" crop="0,0,1,1" id="ubf7f18eb" class="ne-image"></p><ol start="2" class="ne-ol"><li id="ufa3609e7" data-lake-index-type="0"><span class="ne-text" style="color: rgb(37, 41, 51); font-size: 16px">服务器生成一个随机 token，存在 session 里，同时放到页面的隐藏字段或请求头中。提交时验证 token 是否匹配。</span></li></ol><p id="u708b0f3d" class="ne-p"><img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1773647940724-a617bf09-9eed-4cb9-870f-29e1e8d90c2c.png" width="663" title="" crop="0,0,1,1" id="u0cd43221" class="ne-image"></p></details>
#### 点击劫持
<font style="color:rgb(37, 41, 51);">是把目标网站用透明 iframe 嵌入到攻击者网站，用户以为在点攻击者的页面，实际点的是 iframe 里的目标网站。</font>

<details class="lake-collapse"><summary id="u9ace5e3c"><span class="ne-text" style="font-size: 16px">解决方案</span></summary><ol class="ne-ol"><li id="u895735ff" data-lake-index-type="0"><span class="ne-text" style="font-size: 16px">X-Frame-Options头</span></li></ol><p id="ud24d28fd" class="ne-p"><img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1773648242860-c0f9e24a-3ba2-4ec3-9bf2-62993af9766e.png" width="441.5" title="" crop="0,0,1,1" id="u33a82b51" class="ne-image"></p><ol start="2" class="ne-ol"><li id="u2719e693" data-lake-index-type="0"><span class="ne-text">CSP设置 frame-ancestors</span></li></ol><p id="ub9203a28" class="ne-p"><img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1773648278186-016e143b-b66e-415c-999e-b870908e29ad.png" width="519" title="" crop="0,0,1,1" id="uc08bbc9a" class="ne-image"></p></details>
#### 依赖安全
<font style="color:rgb(37, 41, 51);">npm 生态有个问题：安装一个包会引入 79 个第三方依赖和 39 个维护者的隐式信任。如果某个依赖被攻击者控制，你的项目就危险了。</font>

<img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1773648480892-83313fdd-31ff-4355-b986-5e5d3acc1cbd.png" width="706.5" title="" crop="0,0,1,1" id="ub0dca76b" class="ne-image">

<details class="lake-collapse"><summary id="uf1178d2e"><span class="ne-text">防御供应链攻击</span></summary><ol class="ne-ol"><li id="u8a29420b" data-lake-index-type="0"><span class="ne-text">锁定依赖版本</span></li></ol><p id="u14a6b8f2" class="ne-p"><img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1773648606372-f3cb3505-54a2-4fa4-b0ec-d814e551d9fc.png" width="654.5" title="" crop="0,0,1,1" id="uc3963ac0" class="ne-image"></p><ol start="2" class="ne-ol"><li id="u7f894225" data-lake-index-type="0"><span class="ne-text">使用npm ci</span></li></ol><p id="u82db3941" class="ne-p"><img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1773648632884-198edee3-db26-4c3b-be95-dcb0a821c29c.png" width="603.5" title="" crop="0,0,1,1" id="u62aa1108" class="ne-image"></p></details>
#### 本地存储的使用安全
<img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1773648677066-0420bfd2-6e84-4216-b4d7-f122c891941f.png" width="649" title="" crop="0,0,1,1" id="u3e09902f" class="ne-image">

#### 安全HTTP响应头
+ CSP

<img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1773649185633-eca1f1c0-4720-4145-a117-8a189d609b29.png" width="449" title="" crop="0,0,1,1" id="u461e1b02" class="ne-image">

+ Referrer-Policy

<img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1773649150975-72afb33f-454a-4eb9-ac79-9c357b58b935.png" width="442.5" title="" crop="0,0,1,1" id="u3c325423" class="ne-image">

+ X-Frame-Options

<img src="https://cdn.nlark.com/yuque/0/2026/png/55003070/1773649224640-a3520712-019c-4e2c-b639-42b1b197bc2d.png" width="447" title="" crop="0,0,1,1" id="u1be4ae6b" class="ne-image">
